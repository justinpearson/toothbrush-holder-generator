TODO
====

- Regenerate README hero image (images/app-screenshot.png): it predates the v2
  UI (shape selector, solid/tube toggle, global defaults + per-object overrides)
  and the filament color picker, and still shows the old "Maker" title.

- Regenerate README demo (images/demo.gif and images/demo.mp4): same — recorded
  before v2. A new demo should show picking shapes (ellipse / polygon / star),
  toggling solid vs tube, and overriding a size on one object.
